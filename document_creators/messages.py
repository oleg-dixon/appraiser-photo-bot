"""Функции для генерации сообщений пользователю."""

from typing import Dict, Optional
from datetime import datetime
from .utils import get_size_option_name, calculate_pages_info


class MessageGenerator:
    """Генератор сообщений для пользователя"""

    # ===== Базовые сообщения =====

    @staticmethod
    def get_unknown_button_message() -> str:
        """Сообщение для неизвестной кнопки."""
        return "Неизвестная команда. Пожалуйста, используйте кнопки из меню."

    @staticmethod
    def get_session_expired_message() -> str:
        """Сообщение об истекшей сессии."""
        return "❌ Сессия устарела. Пожалуйста, нажмите '🟢 Начать' для начала."

    @staticmethod
    def get_no_active_session_message() -> str:
        """Сообщение об отсутствии активной сессии."""
        return "Нет активной сессии. Нажмите '🟢 Начать' для начала работы."

    @staticmethod
    def get_initial_bot_message() -> str:
        """Начальное сообщение бота при запуске."""
        return (
            "🖼️ *Многостраничный Фото-Бот*\n\n"
            "Я создам Word документ из ваших фотографий с невидимыми границами.\n\n"
            "📌 *Новый функционал:*\n"
            "✅ *Многостраничность* - загружайте сколько угодно фото\n"
            "✅ *Автораспределение* - фото автоматически разбиваются на страницы\n"
            "✅ *Гибкие таблицы* - каждая страница отдельная таблица\n\n"
            "📝 Введите заголовок для таблицы (или нажмите 'Без заголовка' если без заголовка):"
        )

    @staticmethod
    def get_title_saved_message(title: str) -> str:
        """Сообщение о сохранении заголовка."""
        return (
            f"✅ Заголовок таблицы сохранен: *{title}*\n\n"
            "Теперь введите количество *СТРОК* в таблице (например, 3):\n\n"
            "*Примечание:* Этот размер будет использоваться для каждой странице"
        )

    @staticmethod
    def get_no_title_message() -> str:
        """Сообщение при отсутствии заголовка."""
        return (
            "✅ Хорошо, без заголовка.\n\n"
            "Теперь введите количество *СТРОК* в таблице (например, 3):\n\n"
            "*Примечание:* Этот размер будет использоваться для каждой странице"
        )

    @staticmethod
    def get_rows_saved_message(rows: int) -> str:
        """Сообщение о сохранении количества строк."""
        return (
            f"✅ Строк на странице: *{rows}*\n\n"
            "Теперь введите количество *СТОЛБЦОВ* в таблице (например, 4):\n\n"
            "*Примечание:* Этот размер будет использоваться для каждой страницы"
        )

    @staticmethod
    def get_validation_rows_message(max_rows: int) -> str:
        """Сообщение о валидации количества строк."""
        return f"⚠️ Для стабильной работы рекомендуется не более {max_rows} строк."

    @staticmethod
    def get_validation_positive_integer_message() -> str:
        """Сообщение об ошибке валидации положительного целого числа."""
        return "❌ Пожалуйста, введите целое положительное число (например, 3):"

    @staticmethod
    def get_validation_columns_message(max_cols: int) -> str:
        """Сообщение о валидации количества столбцов."""
        return f"⚠️ Для лучшего качества рекомендуется не более {max_cols} столбцов."

    @staticmethod
    def get_too_many_photos_per_page_message(
        photos_per_page: int, max_photos: int
    ) -> str:
        """Сообщение о слишком большом количестве фото на странице."""
        return (
            f"❌ Слишком много фото на странице ({photos_per_page}). "
            f"Максимально разрешено: {max_photos}"
        )

    @staticmethod
    def get_no_photos_error() -> str:
        """Сообщение об ошибке: нет фото."""
        return (
            "❌ Вы не загрузили ни одной фотографии.\n"
            "Отправьте фото или нажмите '🟢 Начать' для начала."
        )

    @staticmethod
    def get_photo_format_error() -> str:
        """Сообщение об ошибке формата фото."""
        return "Пожалуйста, отправьте фото в формате JPG или PNG"

    @staticmethod
    def get_photo_processing_error() -> str:
        """Сообщение об ошибке обработки фото."""
        return "❌ Ошибка при обработке фото. Попробуйте отправить фото снова."

    @staticmethod
    def get_size_selection_message(rows: int, cols: int) -> str:
        """Сообщение для выбора размера фото."""
        photos_per_page = rows * cols
        return (
            f"✅ Столбцов на странице: *{cols}*\n"
            f"📊 Размер таблицы на странице: *{rows}×{cols}*\n"
            f"📍 Фото на странице: *{photos_per_page}*\n\n"
            "📏 Выберите размер фотографий в таблице:"
        )

    @staticmethod
    def get_upload_instructions(rows: int, cols: int, size_text: str) -> str:
        """Инструкции для загрузки фото."""
        photos_per_page = rows * cols
        return (
            f"✅ Размер фото: *{size_text}*\n\n"
            f"📍 Фото на странице: *{photos_per_page}*\n\n"
            f"📌 *Как загружать фото:*\n"
            f"1. Отправляйте фото по одному или пачками\n"
            f"2. Будет создана столько страниц, сколько нужно\n"
            f"3. Каждая страница - таблица {rows}×{cols}\n"
            f"4. Когда все фото загружены, нажмите '✅ Готово'\n\n"
            f"*Отправьте первое фото или нажмите '✅ Готово'*"
        )

    @staticmethod
    def generate_upload_progress(current: int, rows: int, cols: int) -> str:
        """Генерирует сообщение о прогрессе загрузки фото."""
        photos_per_page = rows * cols

        if photos_per_page > 0:
            current_page = (current - 1) // photos_per_page + 1 if current > 0 else 1

            progress_on_page = current % photos_per_page
            if progress_on_page == 0 and current > 0:
                progress_on_page = photos_per_page

            progress = int(progress_on_page / photos_per_page * 20)
            progress_bar = "🟩" * progress + "⬜" * (20 - progress)
        else:
            current_page = 1
            progress_bar = ""
            progress_on_page = current
            photos_per_page = current

        page_info = calculate_pages_info(current, rows, cols)

        return (
            f"📥 *Получено фото: {current}*\n"
            f"📄 *Страница: {current_page}/{page_info['total_pages']}*\n"
            f"📍 *На этой странице: {progress_on_page}/{photos_per_page}*\n"
            f"{progress_bar}\n\n"
            f"📋 *Всего будет страниц: {page_info['total_pages']}*\n"
            f"🔄 *Отправляйте ещё фото или нажмите '✅ Готово'*"
        )

    @staticmethod
    def get_confirmation_message(
        title: Optional[str],
        photos_count: int,
        rows: int,
        cols: int,
        size_option: str,
        page_info: Dict,
    ) -> str:
        """Создает сообщение подтверждения перед созданием документа."""
        size_text = get_size_option_name(size_option)
        stats = MessageGenerator.generate_document_stats(photos_count, rows, cols, page_info)

        return (
            f"✅ *Загрузка завершена!*\n\n"
            f"📊 *Статистика документа:*\n"
            f"{stats}\n\n"
            f"📝 *Дополнительно:*\n"
            f"• Заголовок: {title or 'нет'}\n"
            f"• Размер фото: {size_text}\n\n"
            f"*Создаем многостраничный документ?*"
        )

    @staticmethod
    def generate_document_stats(
        photos_count: int, rows: int, cols: int, page_info: Dict
    ) -> str:
        """Генерирует статистику документа для сообщения пользователю."""
        stats = []

        if photos_count == 1:
            stats.append("📷 Фотографий: 1")
        else:
            stats.append(f"📷 Фотографий: {photos_count}")

        stats.append(f"📊 Таблица: {rows}×{cols}")
        stats.append(f"📍 Фото на странице: {page_info['photos_per_page']}")

        if page_info["total_pages"] == 1:
            stats.append("📄 Страниц: 1")
        else:
            stats.append(f"📄 Страниц: {page_info['total_pages']}")
            if page_info["photos_on_last_page"] != page_info["photos_per_page"]:
                stats.append(
                    f"📌 На последней странице: {page_info['photos_on_last_page']} фото"
                )

        return "\n".join(stats)

    @staticmethod
    def get_back_to_size_message(rows: int, cols: int) -> str:
        """Сообщение о возврате к выбору размера."""
        photos_per_page = rows * cols
        return (
            f"Возвращаю к выбору размера.\n\n"
            f"✅ Размер таблицы: *{rows}×{cols}*\n"
            f"📍 Фото на странице: *{photos_per_page}*\n\n"
            "📏 Выберите размер фотографий в таблице:"
        )

    @staticmethod
    def get_back_to_columns_message(rows: int) -> str:
        """Сообщение о возврате к вводу столбцов."""
        return (
            f"Возвращаю к настройке таблицы.\n\n"
            f"✅ Строк на странице: *{rows}*\n\n"
            "Теперь введите количество *СТОЛБЦОВ* в таблице (например, 4):\n\n"
            "*Примечание:* Этот размер будет использоваться для каждой страницы"
        )

    @staticmethod
    def get_back_to_rows_message() -> str:
        """Сообщение о возврате к вводу строк."""
        return (
            "Возвращаю к началу настройки таблицы.\n\n"
            "Введите количество *СТРОК* в таблице (например, 3):\n\n"
            "*Примечание:* Этот размер будет использоваться для каждой страницы"
        )

    @staticmethod
    def get_back_to_title_message() -> str:
        """Сообщение о возврате к началу."""
        return (
            "Начинаем заново.\n\n"
            "📝 Введите заголовок для таблицы (или нажмите 'Без заголовка' если без заголовка):"
        )

    @staticmethod
    def get_back_confirmation_message() -> str:
        """Сообщение подтверждения для возврата."""
        return (
            "⚠️ *Внимание!*\n\n"
            "У вас есть загруженные фотографии. Возврат приведет к их удалению.\n\n"
            "Вы уверены, что хотите вернуться назад?"
        )

    @staticmethod
    def get_return_cancelled_message() -> str:
        """Сообщение об отмене возврата."""
        return (
            "Возврат отменен. Продолжаем работу с текущими фотографиями.\n\n"
            "Вы можете загрузить еще фото или нажать '✅ Готово'."
        )

    @staticmethod
    def get_photos_deleted_message() -> str:
        """Сообщение об удалении фотографий."""
        return "✅ Фотографии удалены."

    @staticmethod
    def get_operation_cancelled_message() -> str:
        """Сообщение об отмене операции."""
        return "Операция отменена. Данные удалены.\nДля начала новой работы нажмите '🟢 Начать'"

    @staticmethod
    def get_confirm_cancelled_message() -> str:
        """Сообщение об отмене подтверждения."""
        return "Операция отменена. Для начала нового документа нажмите '🟢 Начать'"

    @staticmethod
    def get_photos_cleared_message(photos_count: int) -> str:
        """Сообщение об очистке фотографий."""
        return (
            f"✅ Очищено {photos_count} фото.\n\n"
            f"Вы можете продолжить загружать фото или нажмите '🟢 Начать' для перезапуска."
        )

    @staticmethod
    def get_no_data_to_clean_message() -> str:
        """Сообщение об отсутствии данных для очистки."""
        return "Нет активной сессии. Нет данных для очистки."

    # ===== Сообщения создания документа с прогрессом =====

    @staticmethod
    def get_creating_document_message_with_progress(
        photos_count: int,
        rows: int,
        cols: int,
        page_info: Dict,
        progress: int = 10,
    ) -> str:
        """Сообщение о начале создания документа с прогрессом."""
        return (
            f"🛠️ *Создаю документ...* ({progress}%)\n\n"
            f"📷 Фото: {photos_count}\n"
            f"📊 Таблица: {rows}×{cols}\n"
            f"📄 Страниц: {page_info['total_pages']}\n"
            f"⏱️ Это займет некоторое время..."
        )

    @staticmethod
    def get_sending_document_message_with_progress(
        doc_size_mb: float, progress: int = 50
    ) -> str:
        """Сообщение о отправке документа с прогрессом."""
        return (
            f"📤 *Отправляю документ...* ({progress}%)\n\n"
            f"📦 Размер: {doc_size_mb:.1f} MB\n"
            f"⏱️ Это может занять некоторое время..."
        )

    @staticmethod
    def get_file_sent_message(progress: int = 80) -> str:
        """Сообщение о том, что файл отправлен с прогрессом."""
        return f"📎 *Файл отправлен* ({progress}%)"

    @staticmethod
    def get_document_success_message_with_progress(
        photos_count: int,
        rows: int,
        cols: int,
        page_info: Dict,
        doc_size_mb: float,
        progress: int = 100,
    ) -> str:
        """Сообщение об успешной отправке документа с прогрессом."""
        return (
            f"✅ *Документ успешно отправлен!* ({progress}%)\n\n"
            f"📊 *Статистика:*\n"
            f"• Фото: {photos_count}\n"
            f"• Таблица на странице: {rows}×{cols}\n"
            f"• Страниц: {page_info['total_pages']}\n"
            f"• Размер файла: {doc_size_mb:.1f} MB\n\n"
            "📦 Фотографии удалены из памяти бота.\n"
            "🔄 Для создания нового документа - '🟢 Начать'"
        )

    @staticmethod
    def get_document_caption(
        title: Optional[str],
        photos_count: int,
        rows: int,
        cols: int,
        size_option: str,
        page_info: Dict,
    ) -> str:
        """Короткая подпись для отправляемого файла."""
        size_text = get_size_option_name(size_option)

        if title:
            caption = f"📎 {title} - {photos_count} фото ({rows}×{cols})"
        else:
            caption = f"📎 {photos_count} фото, таблица {rows}×{cols}"

        caption += f" | 📏 {size_text} | 📄 {page_info['total_pages']} стр."
        return caption

    @staticmethod
    def generate_filename(
        title: Optional[str], photos_count: int, rows: int, cols: int
    ) -> str:
        """Генерирует имя файла для документа."""
        timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")

        if title:
            clean_title = "".join(
                c for c in title if c.isalnum() or c in (" ", "-", "_")
            ).strip()
            clean_title = clean_title.replace(" ", "_")
            return f"{clean_title}_{photos_count}photos_{rows}x{cols}_{timestamp}.docx"
        else:
            return f"photos_{photos_count}_{rows}x{cols}_{timestamp}.docx"

    # ===== Статус сообщения =====

    @staticmethod
    def get_bot_status_message(
        total_users: int, total_photos: int, current_time: str
    ) -> str:
        """Общий статус бота."""
        return (
            f"📊 *Общий статус бота:*\n\n"
            f"• Активных сессий: {total_users}\n"
            f"• Фото в памяти: {total_photos}\n"
            f"• Время: {current_time}\n\n"
            f"Для начала работы нажмите '🟢 Начать'"
        )

    @staticmethod
    def get_session_status_upload_message(
        photos_count: int,
        rows: int,
        cols: int,
        photos_per_page: int,
        total_pages: int,
        title: Optional[str],
    ) -> str:
        """Статус сессии при загрузке фото."""
        return (
            f"📊 *Статус вашей сессии:*\n\n"
            f"• Состояние: *Загрузка фото*\n"
            f"• Загружено фото: *{photos_count}*\n"
            f"• Размер таблицы: *{rows}×{cols}*\n"
            f"• Фото на странице: *{photos_per_page}*\n"
            f"• Страниц будет: *{total_pages}*\n"
            f"• Заголовок: {title or 'нет'}\n\n"
            f"*Действия:*\n"
            f"• Продолжайте загружать фото\n"
            f"• Или нажмите '✅ Готово'"
        )

    @staticmethod
    def get_session_status_size_selection_message(
        rows: int, cols: int, title: Optional[str]
    ) -> str:
        """Статус сессии при выборе размера."""
        return (
            f"📊 *Статус вашей сессии:*\n\n"
            f"• Состояние: *Выбор размера фото*\n"
            f"• Размер таблицы: {rows}×{cols}\n"
            f"• Заголовок: {title or 'нет'}\n\n"
            f"Выберите размер фото из меню выше."
        )

    @staticmethod
    def get_session_status_table_setup_message(title: Optional[str]) -> str:
        """Статус сессии при настройке таблицы."""
        return (
            f"📊 *Статус вашей сессии:*\n\n"
            f"• Состояние: *Настройка таблицы*\n"
            f"• Заголовок: {title or 'нет'}\n\n"
            f"Следуйте инструкциям для настройки таблицы."
        )

    @staticmethod
    def get_session_status_title_message() -> str:
        """Статус сессии при вводе заголовка."""
        return (
            "📊 *Статус вашей сессии:*\n\n"
            "• Состояние: *Ввод заголовка*\n\n"
            "Введите заголовок или нажмите 'Без заголовка'."
        )

    @staticmethod
    def get_session_status_ready_message() -> str:
        """Статус сессии при готовности к работе."""
        return (
            "📊 *Статус вашей сессии:*\n\n"
            "• Состояние: *Готов к работе*\n\n"
            "Для начала нажмите '🟢 Начать'."
        )

    # ===== Помощь =====

    @staticmethod
    def get_help_message(max_rows: int, max_cols: int, max_photos: int) -> str:
        """Справка по боту."""
        return (
            "🆘 *Помощь по многостраничному боту:*\n\n"
            "*Основные кнопки:*\n"
            "🟢 *Начать* - начать создание документа\n"
            "✅ *Готово* - завершить загрузку фото (появляется на этапе загрузки)\n"
            "◀️ *Назад* - вернуться к предыдущему шагу\n"
            "🧹 *Очистить* - очистить загруженные фото\n"
            "📊 *Статус* - показывает текущий статус\n"
            "❓ *Помощь* - эта справка\n"
            "📝 *Без заголовка* - пропустить ввод заголовка\n"
            "✅ *Да, всё верно* - подтвердить создание документа\n"
            "❌ *Нет, начать заново* - отменить и начать заново\n\n"
            "*Процесс работы:*\n"
            "1. Нажмите '🟢 Начать'\n"
            "2. Введите заголовок или нажмите 'Без заголовка'\n"
            "3. Введите количество строк в таблице (например, 3)\n"
            "4. Введите количество столбцов (например, 4)\n"
            "5. Выберите размер фото из меню\n"
            "6. Загружайте фото по одному\n"
            "7. Нажмите '✅ Готово' когда все фото загружены\n"
            "8. Подтвердите создание документа кнопкой '✅ Да, всё верно'\n"
            "9. Получите готовый Word-файл\n\n"
            "*Особенности:*\n"
            "• *Многостраничность* - фото автоматически распределяются по страницам\n"
            "• *Гибкие таблицы* - каждая страница содержит отдельную таблицу\n"
            "• *Невидимые границы* - таблица в документе без видимых границ\n"
            "• *Автосжатие* - фото автоматически оптимизируются\n\n"
            f"*Ограничения:*\n"
            f"• Максимум строк на странице: {max_rows}\n"
            f"• Максимум столбцов на странице: {max_cols}\n"
            f"• Максимум фото на странице: {max_photos}\n\n"
        )

    # ===== Сообщения об ошибках =====

    @staticmethod
    def get_too_many_photos_error(
        photos_count: int, max_photos: int = 15
    ) -> str:
        """Сообщение об ошибке: слишком много фото."""
        return (
            f"⚠️ *Слишком много фото ({photos_count})*\n\n"
            f"Для стабильной работы рекомендуется не более {max_photos} фото за раз.\n\n"
            f"📋 *Что делать:*\n"
            f"1. Удалите часть фото (оставьте не более {max_photos})\n"
            f"2. Используйте команду /done снова\n"
            f"3. Или нажмите '🟢 Начать' для начала заново"
        )

    @staticmethod
    def get_document_too_big_error(doc_size_mb: float) -> str:
        """Сообщение об ошибке: документ слишком большой."""
        return (
            f"⚠️ *Документ слишком большой ({doc_size_mb:.1f} MB)*\n\n"
            f"Telegram не может отправить файлы больше 50MB.\n\n"
            f"📋 *Рекомендации:*\n"
            f"• Используйте до 10 фото за раз\n"
            f"• Выбирайте 'Маленький' размер фото\n"
            f"• Отправляйте фото меньшего разрешения\n"
            f"• Разделите фото на несколько документов\n\n"
            f"🔄 Попробуйте снова с меньшим количеством фото, нажмите: '🟢 Начать'"
        )

    @staticmethod
    def get_timeout_error(doc_size_mb: float) -> str:
        """Сообщение об ошибке таймаута."""
        return (
            "⏳ *Отправка занимает слишком много времени*\n\n"
            f"Документ ({doc_size_mb:.1f} MB) слишком большой для быстрой отправки.\n\n"
            "📋 *Что делать:*\n"
            "1. Подождите несколько минут - файл может быть доставлен\n"
            "2. Попробуйте с меньшим количеством фото (до 5-10)\n"
            "3. Используйте фото меньшего размера\n"
            "4. Выбирайте 'Маленький' размер в настройках\n\n"
            "🔄 Начните заново: /start"
        )

    @staticmethod
    def get_file_too_big_error(doc_size_mb: float) -> str:
        """Сообщение об ошибке: файл слишком большой для Telegram."""
        return (
            "📦 *Файл слишком большой*\n\n"
            f"Документ ({doc_size_mb:.1f} MB) превышает лимиты Telegram.\n\n"
            "📋 *Лимиты Telegram:*\n"
            "• Максимальный размер файла: ~50MB\n"
            "• Рекомендуемый размер: до 20MB\n\n"
            "🔄 Попробуйте снова с меньшим количеством фото, нажмите: '🟢 Начать'"
        )

    @staticmethod
    def get_generic_api_error(error_msg: str) -> str:
        """Сообщение об общей ошибке Telegram API."""
        return (
            f"❌ *Ошибка Telegram API:*\n\n"
            f"`{error_msg[:200]}...`\n\n"
            f"🔄 Попробуйте снова, нажмите: '🟢 Начать'"
        )

    @staticmethod
    def get_creation_timeout_error() -> str:
        """Сообщение об ошибке таймаута создания."""
        return (
            "⏳ *Процесс создания занял слишком много времени*\n\n"
            "Создание документа с таким количеством фото требует много времени.\n\n"
            "📋 *Рекомендации:*\n"
            "• Используйте до 10 фото за раз\n"
            "• Выбирайте меньший размер фото\n"
            "• Отправляйте фото меньшего разрешения\n\n"
            "🔄 Попробуйте снова, нажмите: '🟢 Начать'"
        )

    @staticmethod
    def get_generic_creation_error(error_msg: str) -> str:
        """Сообщение об общей ошибке создания документа."""
        return (
            f"❌ *Ошибка при создании документа:*\n\n"
            f"`{error_msg[:200]}...`\n\n"
            f"📋 *Возможные причины:*\n"
            f"• Слишком много фото\n"
            f"• Недостаточно памяти\n"
            f"• Проблемы с форматом фото\n\n"
            f"🔄 Попробуйте снова, нажмите: '🟢 Начать'"
        )

    @staticmethod
    def get_start_prompt() -> str:
        """Сообщение-приглашение начать работу."""
        return "Для начала нажмите '🟢 Начать'"


message_generator = MessageGenerator()


def generate_document_stats(*args, **kwargs) -> str:
    return MessageGenerator.generate_document_stats(*args, **kwargs)


def get_photo_upload_progress(*args, **kwargs) -> str:
    return MessageGenerator.generate_upload_progress(*args, **kwargs)
