# Appraiser Table Bot

Telegram бот для создания Word документов с таблицами из фотографий. Создавался для нужд оценочного сообщества.

## Возможности
- 📷 Загрузка фотографий через Telegram
- 📊 Создание таблиц с невидимыми границами
- 📝 Добавление заголовка таблицы
- ⚙️ Настройка количества строк и столбцов
- 📏 Автоматический подбор размера фото
- 🔧 Ручной выбор размера (маленький, средний, большой)
- 🗑️ Автоматическая очистка данных после создания документа
- 🔒 Сжатие изображений для уменьшения размера файла


## Быстрый старт
1. Клонируйте репозиторий:
```bash
git clone https://github.com/oleg-dixon/appraiser-photo-bot.git
cd appraiser-photo-bot
```

2. Инициализируйте проект (создает .env, устанавливает зависимости):
```bash
make init
```

3. Отредактируйте файл .env, добавив токен бота:
```bash
nano .env
```

4. Запустите бота
```bash
make run
```


## Команды бота
* /start - начать создание таблицы
* /cleanup - очистить ваши данные из памяти
* /status - показать статус бота
* /help - показать справку


## Процесс работы
1. Запустите бота командой /start
2. Ответьте, хотите ли вы добавить заголовок
3. Укажите количество строк и столбцов
4. Выберите размер фотографий
5. Загрузите фотографии (количество должно соответствовать ячейкам таблицы)
6. Подтвердите создание документа
7. Получите готовый Word файл


## Требования
* Python 3.12 или выше
* UV (рекомендуется) или pip
* Telegram Bot Token (получить у @BotFather)


## Установка
С UV (рекомендуется)
```bash
# Установите UV (если еще не установлен)
curl -LsSf https://astral.sh/uv/install.sh | sh

# Установите зависимости
make install
```

С pip (альтернатива)
```bash
pip install -r requirements.txt
```

Установка для разработки
```bash
make install-dev
```


### Использование
Основные команды
```bash
# Показать все доступные команды
make help

# Запустить бота
make run
# или
make bot

# Запустить с проверкой окружения
make check-env && make run
```


## Разработка
Установка для разработки
```bash
make install-dev
```

Доступные команды Makefile
```bash
# Форматирование кода
make format

# Проверка кода
make lint
make type-check

# Запуск тестов
make test

# Все проверки сразу
make check-all

# Сборка пакета
make build

# Очистка временных файлов
make clean
```


## Тестирование
```bash
# Запустить все тесты
make test

# Запустить тесты с покрытием
pytest --cov=appraiser_photo_bot --cov-report=html
```


## Форматирование кода
Проект использует:
* Black для форматирования
* isort для сортировки импортов
* flake8 для проверки стиля
* mypy для проверки типов

```bash
# Автоматическое форматирование
make format

# Проверить форматирование без изменений
make format-check
```


## Docker
```bash
# Собрать образ
make docker-build

# Запустить контейнер
make docker-run

# Очистить образы
make docker-clean
```

## Вклад в проект
1. Форкните репозиторий
2. Создайте ветку для новой функции (git checkout -b feature/amazing-feature)
3. Зафиксируйте изменения (git commit -m 'Add amazing feature')
4. Запушьте ветку (git push origin feature/amazing-feature)
5. Откройте Pull Request


## Стандарты кода
* Используйте make format перед коммитом
* Добавляйте тесты для новой функциональности
* Обновляйте документацию при необходимости
* Следуйте существующему стилю кода

## Лицензия
Распространяется под лицензией MIT


## Поддержка
Если вы нашли ошибку или у вас есть предложение по улучшению, создайте issue.


## Благодарности
* python-telegram-bot
* python-docx
* Pillow
* UV от Astral

