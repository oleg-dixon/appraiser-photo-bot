[![Python CI](https://github.com/oleg-dixon/appraiser-photo-bot/actions/workflows/pyci.yml/badge.svg)](https://github.com/oleg-dixon/appraiser-photo-bot/actions/workflows/pyci.yml)

# 📸 Appraiser Table Bot

Telegram бот для создания Word документов с таблицами из фотографий. Разработан оценщиком СРО «РАО» Олегом Олеговичем Дюкаревым для оценочного и экспертного сообществ.

## ✨ Возможности
- 📷 Загрузка фотографий через Telegram
- 📊 Создание таблиц с невидимыми границами
- 📝 Добавление заголовка таблицы
- ⚙️ Настройка количества строк и столбцов
- 📏 Автоматический подбор размера фото
- 🔧 Ручной выбор размера (маленький, средний, большой)
- 🗑️ Автоматическая очистка данных после создания документа
- 🔒 Сжатие изображений для уменьшения размера файла


## 🚀 Быстрый старт
**1. Клонируйте репозиторий:**
```bash
git clone https://github.com/oleg-dixon/appraiser-photo-bot.git
cd appraiser-photo-bot
```

**2. Установите UV (если еще не установлен)**
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh

# Или 
pip install uv
```

**3. Инициализируйте проект (создает виртуальное окружение, устанавливает зависимости, настраивает .env):**
```bash
make init
```

**4. Отредактируйте файл .env, добавив токен от [@BotFather](https://t.me/botfather):**
```bash
nano .env
# Добавьте: BOT_TOKEN=ваш_токен_бота
```

**5. Запустите бота**
```bash
make run
```

## 🤖 Команды бота
* /start - начать создание таблицы
* /cleanup - очистить ваши данные из памяти
* /status - показать статус бота
* /help - показать справку


## 📋 Процесс работы
1. Запустите бота командой /start
2. Ответьте, хотите ли вы добавить заголовок
3. Укажите количество строк и столбцов
4. Выберите размер фотографий
5. Загрузите фотографии (количество должно соответствовать ячейкам таблицы)
6. Подтвердите создание документа
7. Получите готовый Word файл


## 🛠️ Требования
* Python 3.12 или выше
* UV (рекомендуется) или pip
* Telegram Bot Token (получить у [@BotFather](https://t.me/botfather))


## 📦 Установка
**С UV (рекомендуется)**
```bash
# Установите UV (если еще не установлен)
make uv-install
```

**Инициализируйте проект**
```bash
make init
```

**С pip (альтернатива)**
```bash
# Создайте виртуальное окружение
python3.12 -m venv .venv
source .venv/bin/activate

# Установите зависимости
pip install -e .
```

### 🚀 Использование
**Основные команды**
```bash
# Показать все доступные команды
make help

# Запустить бота (без проверок)
make run

# Запустить бота (с проверкой окружения)
make bot
```

## 🛠️ Разработка
**Установка для разработки**
```bash
make install-dev
```

## 📋 Доступные команды Makefile

### 🚀 Начало работы
```bash
make help           # Показать все доступные команды
make init           # Инициализация проекта (первый запуск)
make venv           # Создать виртуальное окружение
make install        # Установить зависимости
make install-dev    # Установить зависимости для разработки
make setup-env      # Настроить файл окружения .env
```
### 🤖 Запуск бота
```bash
make run            # Запустить бота
make bot            # Запустить бота с проверкой окружения
```
### 🔍 Проверка и форматирование кода
```bash
make lint           # Проверить код с помощью ruff
make format         # Форматировать код с помощью ruff
make format-imports # Отсортировать импорты
make quick-check    # Отсортировать импорты и отформатировать код
```
### 🐳 Docker команды
```bash
make docker-build   # Собрать Docker образ
make docker-run     # Запустить в Docker
make docker-clean   # Очистить Docker образы
make docker-down    # Остановить Docker контейнеры
make docker-logs    # Показать логи Docker
make docker-shell   # Зайти в shell Docker контейнера
```
### 🧹 Очистка
```bash
make clean          # Очистить временные файлы и кэш
```

### ℹ️ Вспомогательные команды
```bash
make version               # Показать версию пакета
make check                 # Проверить систему
make check-python-version  # Проверить версию Python
make uv-install            # Установить uv (если не установлен)
```

### 🔄 Рабочие процессы
**Первый запуск проекта**
```bash
git clone https://github.com/oleg-dixon/appraiser-photo-bot.git
cd appraiser-photo-bot
make uv-install
make init
# Отредактируйте .env файл
make bot
```

**Ежедневная разработка**
```bash
source .venv/bin/activate
make quick-check    # Отформатировать код и отсортировать импорты
make bot            # Запустить бота для тестирования
```

**Перед коммитом**
```bash
make quick-check    # Отформатировать код и отсортировать импорты
make lint           # Проверить код на ошибки
```

**Полная переустановка проекта**
```bash
make clean         # Очистить все временные файлы
make init          # Инициализировать заново
```

### 🐳 Запуск в Docker
**Сборка и запуск**
```bash
make docker-build  # Собрать образ
make docker-run    # Запустить образ
```

**Работа с Docker**
```bash
make docker-logs   # Просмотр логов
make docker-shell  # Вход в контейнер
make docker-down   # Остановка контейнеров
make docker-clean  # Очистка образов
```

## 📁 Структура проекта
```text
appraiser-photo-bot/
├── appraiser_photo_bot/     # Основной пакет
│   ├── __init__.py
│   ├── bot.py              # Основная логика бота
│   ├── config.py           # Конфигурация
│   ├── handlers.py         # Обработчики команд
│   ├── keyboards.py        # Клавиатуры Telegram
│   └── document_creators/  # Создание документов
├── cli.py                  # Точка входа
├── Makefile                # Автоматизация команд
├── pyproject.toml          # Конфигурация проекта
├── .env.example            # Пример переменных окружения
└── README.md               # Документация
```

## 🤝 Вклад в проект
1. Форкните репозиторий
2. Создайте ветку для новой функции (git checkout -b feature/amazing-feature)
3. Зафиксируйте изменения (git commit -m 'Add amazing feature')
4. Запушьте ветку (git push origin feature/amazing-feature)
5. Откройте Pull Request

### Стандарты кода
* Используйте make quick-check перед коммитом
* Следуйте существующему стилю кода
* Обновляйте документацию при необходимости

## 📄 Лицензия
Распространяется под лицензией MIT

## 📬 Контакты
* Автор: Олег Дюкарев (Оценщик СРО «РАО»)
* Email: olegdixon5973@gmail.com
* GitHub: @oleg-dixon

## 🙏 Благодарности
* [python-telegram-bot](https://github.com/python-telegram-bot/python-telegram-bot)
* [python-docx](https://github.com/python-openxml/python-docx)
* [Pillow](https://github.com/python-pillow/Pillow)
* [UV](https://github.com/astral-sh/uv)
* [Ruff](https://github.com/astral-sh/ruff)

## ⭐ Если проект оказался полезным, поставьте звезду на GitHub!

